# Archivos del sistema
.DS_Store
**/.DS_Store

# Screenshot original interno — NO publicar: muestra la interfaz de software
# clínico interno (HorusFit) con un identificador de paciente visible. Solo
# el recorte ya procesado (images/innovacion-horus-proceso-digital.png) se
# usa en index.html y sí se publica.
images/Screenshot 2026-08-28 at 15.46.22.png

# Zip con la familia Geometria completa — index.html no lo referencia (usa
# los .ttf sueltos que ya están en fonts/). No aporta nada al sitio publicado
# y solo duplica archivos de fuente innecesariamente.
fonts/geometria.zip

# Screenshot original de la foto de Karla — index.html usa la versión ya
# procesada (images/historias-karla-rojas.png, con nitidez/contraste
# ajustados). Este archivo es un duplicado sin usar, no tiene nada sensible,
# solo se excluye para no subir un binario redundante.
images/Screenshot 2026-09-01 at 11.15.30.png
